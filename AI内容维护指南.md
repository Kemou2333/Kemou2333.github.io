# Kemou 博客：AI 新增内容操作指南

这份文档用于交给 Codex、Kimi、Claude Code 等编程 AI。目标是让不同 AI 添加文章时使用同一套目录、分类、配色和发布流程，不再临时猜测。

## 1. 开始前

1. 正式项目目录是当前 Git 仓库，不要从旧压缩包或交接副本继续修改。
2. 先检查当前 Git 状态；已有未提交内容默认属于用户，不得覆盖或删除。
3. 阅读根目录 `AGENTS.md`。涉及美术修改时，再阅读 `设计思路与注意事项.md`。
4. 不要编辑 `dist/`、`.astro/` 和 `node_modules/`。

## 2. 判断内容类型

博客不需要为“长文、短文、随笔”分别开发页面，它们都是 Markdown 内容：

- AI 使用体验、普通用户视角、工具记录：`AI 与工具`
- 大学课程、学习方法、个人项目：`学习与项目`
- PC、NS、掌机、游戏体验：`游戏与体验`
- 做饭、通勤、生活感想、短随笔：`生活随记`

只有现有四类确实都不合适时才建立新分类。新分类直接写进 `category` 即可，分类页、计数、归档和 RSS 会自动生成。

## 3. 建立文章文件

文章文件放在：

```text
src/content/posts/
```

可以运行：

```powershell
pnpm new-post -- article-slug
```

也可以直接创建 `article-slug.md`。文件名只使用小写英文、数字和连字符，例如：

```text
first-week-with-codex.md
talos-ending-thoughts.md
subway-handheld-notes.md
```

不要用日期、中文、空格或随意生成的长串作为文件名。文章公开后不要无故改名，因为文件名就是网址的一部分。

## 4. Frontmatter 标准

每篇文章开头使用以下格式：

```markdown
---
title: 文章标题
published: 2026-07-18
description: 用一到两句话说明文章谈什么，不写空泛宣传语。
image: /assets/images/example-cover.webp
tags: [AI, Codex, 普通用户]
category: AI 与工具
draft: false
---

正文从这里开始。
```

字段规则：

- `title`：尊重 Kemou 原本的表达。可以清理明显口语病句，但不能擅自改成营销号标题。
- `published`：首次发布时间，格式固定为 `YYYY-MM-DD`。
- `updated`：只有内容经过实质修改时才添加，格式同上。
- `description`：约 30–80 个汉字，说明具体内容；不要写“本文将带你深入探索”等 AI 套话。
- `image`：没有封面时写空字符串 `''`，不要编造不存在的路径。
- `tags`：建议 2–5 个，复用已有标签优先，避免同义词重复。
- `category`：优先使用上面的四个主分类。
- `draft`：未完成时为 `true`；Kemou确认发布后改为 `false`。
- `lang`：中文文章通常留空即可。

## 5. 图片处理

图片统一放在：

```text
public/assets/images/
```

建议：

- 文件名使用小写英文和连字符。
- 照片或复杂插图优先使用 WebP；需要透明背景时使用 PNG。
- 封面尽量保持横向比例，避免体积异常大的原图直接上线。
- 正文图片必须写有意义的替代文字：

```markdown
![桌面上的掌机与笔记本电脑](/assets/images/study-desk.webp)
```

- 不要把图片转成 Base64 塞进 Markdown，也不要把临时截图留在项目根目录。

## 6. 标题和分类颜色：AI 不需要选择

每篇文章**不设置独立颜色**。颜色完全由统一模块自动决定：

```text
src/utils/accent-utils.ts
```

固定分类对应关系：

| 分类 | 自动强调色 | 用途倾向 |
| --- | --- | --- |
| AI 与工具 | 青蓝 | 科技、工具、Agent |
| 学习与项目 | 蓝紫 | 学习、课程、项目 |
| 游戏与体验 | 珊瑚橙 | 游戏、娱乐、体验 |
| 生活随记 | 薄荷绿 | 生活、通勤、随笔 |

文章卡片、分类侧栏和归档条目都会读取同一个规则。新分类会按分类名称稳定分配现有调色板中的颜色，同一个名称不会每次变化。

因此 AI 不得：

- 在 frontmatter 增加 `color`、`accent` 等自定义字段。
- 给标题写行内 `style="color: ..."`。
- 为某一篇文章新增十六进制色值。
- 因为个人审美临时调换四个主分类的颜色。

只有 Kemou 明确要求“调整整个分类配色”时，才修改统一模块。

## 7. 写入正文

- 保留 Kemou 的第一人称和普通用户视角，不要把他改写成专业程序员。
- 可以修正错字、标点和明显重复，但大幅改写前先征得同意。
- 标题从 `##` 开始；页面会自动把 frontmatter 的 `title` 当作一级标题。
- 普通段落、列表、引用、代码块和图片使用标准 Markdown。
- 不要在文章里解释网站的设计过程，除非文章主题本身就是网站开发记录。

## 8. 完成后的检查

完成一批内容后：

1. 确认 frontmatter 能正常解析，图片路径真实存在。
2. 运行一次 `pnpm.cmd build`。
3. 构建成功即可停止；不要反复刷新、重复构建或进行与本次内容无关的全面重构。
4. 向 Kemou 简要说明新增了哪些文章、使用了什么分类、是否仍为草稿。

## 9. 发布边界

- 如果 Kemou只要求“先写进本地”，不要推送 GitHub。
- 如果 Kemou明确要求上线，才提交本次相关文件并推送到现有 `main` 分支。
- 推送前先同步远端，禁止覆盖其他人的新提交。
- 推送后确认一次 GitHub Pages 发布成功即可，不要没完没了地轮询。

## 10. 最简交接模板

当 Kemou把一篇文章交给 AI 时，AI 至少确认或合理判断以下内容：

```text
标题：
正文：
分类：四个主分类之一
标签：2–5 个
封面：有 / 无
状态：草稿 / 立即发布
```

信息缺失但不影响内容真实性时可以使用保守默认值；涉及是否公开、图片版权或大幅改写观点时，不得擅自决定。
